@model IEnumerable<PMS.Models.DescricaoOcorrenciaSec>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Observacao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Observacao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DescricaoOcorrenciaSecId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DescricaoOcorrenciaSecId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DescricaoOcorrenciaSecId })
        </td>
    </tr>
}

</table>
